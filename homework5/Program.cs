using System;
using System.Linq;

namespace homework5
{
    class Program
    {
        static void Main(string[] args)
        {
            // var arr = new[] {"h", "e"};
            // Console.WriteLine(ArrayHelper.Pop(ref arr));
            // foreach (var item in arr)
            // {
            //     Console.WriteLine($"item: {item}");
            // }

            // var arr = new[] {"h", "e"};
            // Console.WriteLine(ArrayHelper.Push(ref arr, "l"));
            // foreach (var item in arr)
            // {
            //     Console.WriteLine($"item: {item}");
            // }

            // var arr = new[] {"h", "e"};
            // Console.WriteLine(ArrayHelper.Shift(ref arr));
            // foreach (var item in arr)
            // {
            //     Console.WriteLine($"item: {item}");
            // }

            // var arr = new[] {"h", "e"};
            // Console.WriteLine(ArrayHelper.UnShift(ref arr, "e"));
            // foreach (var item in arr)
            // {
            //     Console.WriteLine($"item: {item}");
            // }
        }
    }
}